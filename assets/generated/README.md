# Generated brand sources

These source images were created with the built-in image generation tool for
the Mint Tulsi identity and social preview. `npm run images:optimize` creates
smaller, correctly cropped website versions in `public` while preserving these
originals.
